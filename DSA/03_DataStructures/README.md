# 03_DataStructures

Canonical home for data structure concepts.`nDocument operations, invariants, complexity, and implementation tradeoffs.`nLink to algorithms and patterns that depend on each structure.`nKeep reusable code in [[Template Index]].

